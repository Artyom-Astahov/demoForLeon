version: "3.7"
services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - 5432:5432

#  app:
#    build: docker
#    depends_on:
#      - db
#    ports:
#      - 8080:8080

volumes:
  db-data:
